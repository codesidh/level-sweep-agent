# Copy to terraform.tfvars (gitignored) and fill in real values.
# Required:
project   = "levelsweep"
location  = "eastus"
tenant_id = "PASTE-AZURE-AD-TENANT-ID"

# Optional resource tags (merged into local.tags).
tags = {
  cost-center = "engineering"
  owner       = "you@example.com"
}

apiVersion: v2
name: projection-service
description: Phase 6 Projection Service — cold-path Spring Boot 3.x Monte Carlo projection engine. Serves POST /projection/run + GET /projection/last/{tenantId}; persists per-run results in Mongo (projections.runs); deterministic-by-seed math.
type: application
version: 0.1.0
appVersion: "0.1.0"

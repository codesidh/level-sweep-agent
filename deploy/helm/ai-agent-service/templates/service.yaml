apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-agent-service.fullname" . }}
  labels:
    {{- include "ai-agent-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-agent-service.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "execution-service.fullname" . }}
  labels:
    {{- include "execution-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "execution-service.selectorLabels" . | nindent 4 }}

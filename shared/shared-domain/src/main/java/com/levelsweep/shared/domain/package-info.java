/**
 * Shared domain types: tenant-aware records, DTOs, and enums used across all services.
 *
 * <p>Phase 0 placeholder. Actual domain primitives (TenantId, TradeId, BarTimeframe,
 * SignalDecision, etc.) are added in Phase 1+.
 */
package com.levelsweep.shared.domain;
